# Builder's Diary — PRD v0.1

작성 2026-09-03. 이 문서는 결정된 것, 버린 것과 그 이유, 아직 안 정한 것을 구분한다.
버린 이유를 남기는 것이 이 문서의 절반이다. 나중에 같은 선택지를 다시 만나기 때문이다.

---

## 1. 무엇을 만드는가

빌더가 AI 도구(ChatGPT, Claude, Cursor, Codex, Claude Code…)로 일하는 동안,
스킬 한 번 호출로 그 작업의 기록을 자동 생성한다. 기록은 프로젝트 / 목표 / 한 일의
3층으로 쌓이고, 모든 층에 URL 과 태그가 붙는다.
이력서 불렛 옆의 아이콘을 누르면 그 주장을 뒷받침하는 기록으로 바로 간다.

## 2. 누구를 위한 것인가

**초기 타겟은 빌더다. 제품을 직접 만드는 사람.**
그들에게 스킬과 MCP 는 이미 기본 도구다. 설치 문턱이 아니라 일상이다.
비개발자는 **나중 시장**이다. 대부분의 직군이 AI 를 업무에 쓰게 되면 그들도
"내가 AI 를 어떻게 쓰는가"를 보여줄 포트폴리오가 필요해진다. 그때 확장한다.
초기에 이 둘을 같이 노리면 캡처 방식이 최소공배수로 무너진다.

- **빌더**. 매일 하는 시도와 판단이 휘발된다.
  나중에 보여주려면 맥락을 처음부터 회상해야 해서 에너지가 너무 든다.
- **리크루터**. AI 로 지원자가 급증했고 코드·디자인이 상향 평준화되어
  포트폴리오 스냅샷으로는 판단이 안 된다. 남은 신호는 비즈니스 판단과 오케스트레이션인데
  그 데이터가 휘발되어 있다.

## 3. 왜 지금 (실측 근거)

2026-09-02 경쟁사 조사(scout) + 판정(judge) + 직접 확인 결과.

- 조사된 8개 제품 중 5개가 GitHub 커밋 기반 **개발자 전용**이다.
  비개발자 빌더 대상 자동 기록 제품은 검색어를 여러 각도로 시도해도 찾지 못했다.
- scout 은 "리크루터 검증 기능 0개"라고 보고했으나 **judge 가 이를 뒤집었다.**
  Activi.dev 에 "Recruiter Dashboard — Share a token-protected link with recruiters" 가 있다.
  (출처: dev.to/alairjt, 창업자 글. 직접 확인함)
- 그러나 실제 화면을 브라우저로 열어보니:
  공개 프로필에 보이는 것은 **숫자 3개(12,518 Commits / 287 Projects / 30 Skills)** 와
  "This portfolio is invite-only — Request Access" 게이트뿐이다.
  디렉토리 등록자는 11명이고 대부분 창업자 도시(Florianópolis) 사람들과 @demo 계정이다.

→ 빈칸은 세 축으로 남아 있다: **공개(게이트 없음) / 즉시(클릭 한 번) / 불렛 단위(사람 단위 아님).**
→ 그리고 공개면에 **숫자가 아니라 판단**이 보인다는 점.

### 리크루터 행동은 이미 바뀌었다 (1차 경험 근거)

주의: 이 비교는 교란되어 있다. 마스터 이력서는 링크만 다른 것이 아니라
더 길고 다르게 쓰였고 **서사가 있다**. 리크루터가 반응한 것이 링크가 아니라
서사 품질이었다면, 이 비교는 12절이 인정한 가장 약한 지점(에이전트가 그 서사를
만들 수 있는가)을 오히려 지지하는 셈이다. 근거로 쓰되 과대해석하지 않는다.

가정이 아니다. 창업자 본인이 겪은 것이다.

- 빌더 채용 공고 대부분이 **"직접 만든 것을 공유하라"** 고 요구한다
- 링크드인에서 "지원자들이 기본 지시사항조차 안 따른다"는 리크루터 불만이 반복적으로 관찰된다
- **비교 결과가 있다.** 창업자는 두 종류의 이력서로 지원해봤다.
  프로젝트별 링크가 걸린 **마스터 이력서**와, 링크 없이 공고에 맞춰 **최적화한 이력서**.
  **마스터 이력서 쪽이 더 잘 먹혔다.** 인상평이 아니라 비교다.
  (n 은 작다. 정확한 지원 건수와 응답률은 기록되어 있지 않다 — 남길 것)
- 포트폴리오에 서사가 있어서 리크루터가 "무엇을 했고, 어떻게 판단했고, 결과가 무엇이었는지"를
  다 볼 수 있다. 리크루터에게 직접 칭찬도 받았다 (이건 보조 근거다).

→ 이 제품은 새로운 행동을 만들어내려는 것이 아니다.
**창업자가 손으로 하던 것을 더 쉽고 전문적으로, 빌더와 리크루터 양쪽에 쓸모 있게 구조화하는 일이다.**
이미 작동하는 것을 제품화한다.

## 4. 데이터 모델

3층 + 태그 + URL + 공개 범위.

| 층 | 이름 | 예시 | PM 용어 |
|---|---|---|---|
| 대 | 프로젝트 | Builder's Diary | project |
| 중 | 목표 | 소셜 로그인 추가 | epic |
| 소 | 한 일 | Google OAuth 붙이고 리디렉트 버그 잡음 | record |

**소분류 카드 하나 = 스킬 호출 한 번이다.**
빌더가 LLM 창에서 작업을 마무리하고 `/builders-diary` 를 부르는 그 시점이 카드 하나의 경계다.
우리가 "어디서 끊을까"를 판단하지 않는다. 빌더의 작업 리듬이 단위를 정한다.
- 같은 목표로 세 번 작업하면 그 목표 아래 카드가 세 장 쌓인다. 정상이다
- 한 세션에 서로 관계없는 일을 여러 개 했으면, 스킬이 두 장으로 나누자고 제안한다
- 하루에 0장도 정상이다. 채우려고 부르지 않는다

- **태그는 세 층 모두에 붙는다.** 검색이 층을 가로질러야 하기 때문이다.
  예: "cli" 로 검색하면 그 태그를 가진 프로젝트·목표·기록이 함께 나온다.
  태그는 두 계열로 나눈다: 마인드셋 계열(end-to-end, 검증 규율…) / 도구 계열(claude code, supabase…).
- **URL 은 세 층 모두에 있다.** 딥링크 위치를 사용자가 고른다.
  불렛에 따라 프로젝트가 맞을 때도, 목표가 맞을 때도, 기록 하나가 맞을 때도 있다.
  부수 효과: 링크드인 글에는 기록 URL, 포트폴리오에는 목표 URL, 프로필에는 프로젝트 URL —
  공유되는 모든 자리가 유입 경로가 된다.
- **식별자는 불변 ID.** 슬러그는 표시용. 이력서에 박힌 링크가 죽으면 그 지원자는 끝난다.
  목표를 합치면 옛 ID 를 새 위치로 리다이렉트한다.
  가이드 페이지는 진짜 삭제되었거나 비공개로 바뀐 경우에만 띄운다.
- **공개 범위는 층마다 토글.** 규칙: **자식은 부모보다 더 공개될 수 없다.**

## 5. 캡처 흐름

빌더가 쓰지 않는다. 이미 오간 대화에서 만든다.

1. 작업 도구 안에서 `/builders-diary` 호출 (스킬 또는 MCP)
2. 컨텍스트에서 **필요한 소스만 발췌.** 원문 전체를 플랫폼에 올리지 않는다
3. CLI 처럼 순차로 확인받는다
   - 대분류: 기존 프로젝트 중 선택 / 새로 만들기
   - 중분류: 기존 목표 중 선택 / 새로 만들기 (LLM 이 상위 3개를 제안)
   - 소분류: LLM 이 제목을 제안하고 사용자가 수정·확인
4. 카드 + 태그 생성. 증빙(스크린샷, 코드 스니펫, 라이브 링크)을 붙인다

**캡처 창(capture window) 문제**: 빌더가 부르는 걸 잊고 대화창을 닫거나 컨텍스트를 비우면
그 작업은 영원히 사라진다. 되살릴 방법이 없다.
→ 부르기를 기억나게 만드는 장치가 필요하다 (미정: 세션 종료 감지, 주기적 넛지, 도구 쪽 훅)

**원칙: 최대한 자동화하고 수정·확인만 요청한다.**
단, LLM 은 **확신할 때만 제안한다.** 애매하면 진짜 질문을 한다.
매번 엔터만 누르게 만들면 몇 주 뒤 분류가 무너지고, 그때는 카드가 수백 개라 못 고친다.

## 6. 증빙 흐름

- 소분류 카드 우상단 "이력서에 증빙하기"
- 누르면 그 경험을 **불렛 문장으로 제시**하고, 원하는 층의 **딥링크 URL** 을 만든다
- 불렛을 먼저 쓰고 근거를 찾는 구조가 아니다. 근거에서 불렛이 나온다
  (반대 구조는 소급 정당화가 되어 "AI 가 써준 것" 의심을 자초한다)

## 7. UI

- 상단: 현재 프로젝트명 (변경 가능) — 대분류
- 좌측: 목표 리스트 — 중분류. "무엇을 하려 했는지"
- 우측: 한 일 — 소분류. 스크린샷 / 코드 스니펫 / 라이브 링크 등 증빙 포함
- 상단 검색: 태그로 층을 가로질러 검색
- 카드 앞면은 인사이트 하나. 디테일은 눌러서 본다
- **사람을 평가하는 형용사를 UI 에 넣지 않는다.** 한 일만 보여주고 판단은 읽는 사람이 한다

## 8. 결정하며 버린 것

| 버린 것 | 이유 |
|---|---|
| 컨텍스트 윈도우 원문 전체를 플랫폼에 업로드 | 검증은 최강이지만 사내 코드·`.env`·고객 데이터가 통째로 올라간다. 회사 다니는 빌더는 못 쓴다. 그리고 로그의 대부분은 툴 출력이라 의도가 없다 — 1차 실패("양은 많은데 의도가 없다")의 재현 |
| 이력서 불렛을 먼저 쓰고 근거를 검색해 붙이기 (RAG) | EvidenceCV 방식. 결론을 정해놓고 근거를 찾는 구조라 AI slop 의심을 자초한다 |
| 사람이 직접 기록 작성 | 적은 노동도 매일 요구하면 안 쓴다. 귀찮고 바쁘고 맥락 회상 비용이 크다 |
| 코드/커밋에서 기록 자동 추출 | 1차 시도(136건)에서 실패. 증거의 63%가 소스 한 줄이라 판단의 근거가 안 됐다. 단위가 시도가 아니라 파일이었다. 판단 기준이 어디에도 안 남는다 |
| 기록 단위 = 시도 1건 (원자 단위) | 사람은 무더기를 못 읽는다. 리크루터가 남의 시도 40건을 읽는 일은 없다 |
| 기록 시점에 목표를 고정 | 1인 빌더는 목표가 자주 바뀐다. 실제로 8/27~9/3 사이 같은 작업이 세 번 다른 목표 아래로 옮겨갔다. → 대신 기존 목표 중 선택 + LLM 제안 |
| 슬러그 기반 URL | 제목 수정·목표 병합에 링크가 깨진다. 이력서의 죽은 링크는 치명적 |

## 9. 언어

**제품에 노출되는 모든 텍스트는 영어다.** 글로벌 서비스다.
UI 라벨, 카드 본문, 태그 이름, 빈 상태 문구, 에러 메시지, `lang` 속성 전부.
빌더가 한국어로 작업했으면 기록 원문은 한국어로 두되, 표시 계층은 영어로 간다.
(이 PRD 등 내부 문서는 한국어로 유지)

## 10. 아직 안 정한 것

- **스크린샷을 어떻게 얻는가.** 에이전트가 돌던 앱을 저절로 캡처하지 않는다. 별도 단계가 필요하고, 증빙의 질을 크게 좌우한다
- **태그 어휘.** 기존 201개(JD에서 추출)를 재사용할지, 새로 만들지. 결이 다르다
- **저장 위치와 계정 모델.** 로컬 발췌 파일을 어떻게 플랫폼으로 넘기고 어디에 두는가
- **비개발자 세션에서 "무엇을 하려 했나"가 선명하게 나오는가.** 코딩 세션은 브랜치명·PR 제목이 있지만 챗 세션은 없다
- **캡처 창을 놓치지 않게 하는 방법** (세션 종료 감지 / 넛지 / 도구 훅)
- **브랜드 토큰.** 피치덱의 색·폰트·간격을 디자인 시스템으로 고정할 것

## 11. 누구를 위해 만드는가 — 결정 (2026-09-03)

**창업자 자신을 위해 만든다. 초기 사용자는 창업자 한 명이다.**

만드는 비용이 싸졌다. 잘 아는 분야에서 자기가 필요한 제품이면 빠르게 만들어서
혼자 써도 된다. 시장 검증을 먼저 하지 않는다.

이 결정이 닫는 것:
- **리크루터에게 물어보지 않는다.** 클릭했는지 조사하지 않는다
- **채택 기준선을 정하지 않는다.** 사용자가 한 명이라 채택률이라는 개념이 없다
- **"신호 단계" 프레이밍을 버린다.** 근시일에 반증 불가라 3절의 방어 논리로 쓰지 않는다.
  장기 타이밍에 대한 관찰로만 남긴다

이 결정이 남기는 것:
- **성공 기준: 9월 8일 이후에도 창업자가 계속 쓰는가.** 2주면 확인된다
- 자기가 안 쓰는 제품은 남도 안 쓴다. 이건 가장 싸고 가장 정직한 테스트다

(2026-09-03 builder × judge 3라운드 토론 결과 — review/debate-round1.md.
그 토론의 남은 이견 두 개가 이 결정으로 소멸했다.)

## 12. 가장 위험한 가정

사용자가 창업자 본인이므로 (11절) 수요 측 위험은 닫혔다.
남은 위험은 전부 **자동화**에 있다. 이것은 builder 와 judge 가 토론 끝에
"유일하게 깔끔하게 반증 가능한 가정"으로 합의한 지점이기도 하다.

**1순위 — 닫힘 (2026-09-03).** "에이전트가 만든 기록이, 사람이 손으로 쓴 서사만큼 읽히는가."
창업자는 이미 매일 LLM 으로 포트폴리오를 만들어 왔고 그것이 실제로 먹혔다.
새로 검증할 것이 아니라 이미 하고 있는 일이다. 제품은 그 과정을 구조화하는 것이다.
아래는 그 판단의 근거였던 원래 서술이다. 기록으로만 남긴다.

창업자의 포트폴리오가 증명한 것은 *이 형식이 리크루터에게 먹힌다*는 것이다.
증명되지 않은 것은 *에이전트가 그 형식을 만들어낼 수 있다*는 것이다.
창업자는 서사를 잘 쓴다. 제품의 주장은 창업자가 아닌 사람에게도 된다는 것이다.

**가장 싼 검증**: 창업자의 기존 포트폴리오 항목 하나를 골라, 그 작업의 원본 대화만 주고
에이전트에게 기록을 만들게 한다. 손으로 쓴 것과 나란히 놓고 비교한다.
리크루터에게 둘 다 보여주고 어느 쪽이 더 읽히는지 묻는다.

**2순위: "에이전트와의 대화에 판단이 실제로 들어 있다."**

이 대화(2026-08-27~09-03)에는 많다. 값나가는 지점이 대부분 사람이 에이전트를 꺾은 자리였다.
그러나 "이 버그 고쳐줘" 로 끝나는 세션에도 있는지는 확인되지 않았다.

**가장 싼 검증**: 빌더 본인의 최근 세션 10개를 열어, 몇 개에서 판단이 오갔는지 센다.
