# Builder's Diary — 기록의 단위

**한 기록 = 한 편의 이야기.** 시도 하나가 아니라, 하나의 흐름이 끝났을 때 그 전체.
예: "헤르메스로 밤에 도는 에이전트 팀 세팅하기" 한 편. 그 안에 여러 시도가 장면으로 들어간다.

## 왜 시도 단위가 아닌가

사람은 무더기 정보를 못 읽는다. 리크루터가 남의 시도 40건을 하나씩 읽는 일은 없다.
읽히는 건 이야기다 — 생각의 흐름을 따라가면서 판단한다.
그리고 빌더는 AI 때문에 하루에 너무 많은 실행과 판단을 한다. 그게 다 휘발된다.

## 왜 사람이 쓰면 안 되는가

적은 노동도 매일 요구하면 안 쓴다. 귀찮고, 바쁘고, 나중에 쓰려면 맥락을 다시 회상해야 한다.
**기록은 에이전트가 만든다.** 스킬(또는 프롬프트)로 만들어서 채팅·코워크·IDE·터미널
어디서든 호출한다. 재료는 코드가 아니라 **이미 오간 대화와 실행 흔적**이다. 거기엔
고민과 판단이 그대로 들어 있다.

1차 시도(자기 레포 자동 태깅, 136건)가 실패한 건 자동이라서가 아니라 **재료가 코드여서**다:
- 증거의 63%가 소스 한 줄. 그 줄만 보고는 태그가 맞는지 알 수 없다
- 단위가 파일이었다. `insights_curation_spec.md` 하나가 태그 6건으로 쪼개졌다
- 판단 기준이 없다. 코드에는 결과만 남는다

## 한 편의 뼈대

머리: 무엇을 하려 했고 왜 그게 만만치 않았나
장면들: 각 장면은 다섯 칸으로 접힌다
  1. 상황 — 앞에 뭐가 있었나
  2. 판단 기준 — 뭘 보면 맞고 뭘 보면 틀린 걸로 칠 것인가 (**실행 전에 정한 것**)
  3. 한 것
  4. 결과 — 숫자 또는 관측. 해석 말고
  5. 틀린 것
꼬리: 다음에 같은 일을 할 때 달라지는 것 → 이게 재사용 가능한 자산(스킬)이 된다

2번 칸이 해자다. 레포에서 추출할 수 없는 유일한 칸이고,
코드 기반 분석을 하는 경쟁 제품은 원리적으로 못 채운다.

## 산출물이 두 개다

같은 한 편이 두 사람에게 다르게 읽힌다.
- 빌더에게: 다음에 같은 일을 할 때 부르는 스킬
- 리크루터에게: 이 사람이 문제를 어떻게 다뤘는지 읽는 한 편

## 아직 안 정한 것

- 한 편의 경계를 누가 긋는가 (에이전트가? 빌더가 "이 건 끝났다"고 말할 때?)
- 장면을 몇 개까지 넣는가
- 태깅을 편 단위로 하는가 장면 단위로 하는가
→ 리서치와 첫 야간 런 이후에 정한다
