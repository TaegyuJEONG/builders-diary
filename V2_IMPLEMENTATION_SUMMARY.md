# Builder's Diary V2 UI 설계 구현 완료 보고서

## 📋 개요

Builder's Diary 웹사이트 UI를 대폭 개선하기 위한 **V2 프로토타입**을 완성했습니다. 폴더 연결 전/후의 이원화된 UX와 새로운 3단 레이아웃을 구현했습니다.

---

## 🎯 구현 사항

### 1️⃣ UI 컴포넌트 재설계 (React/Next.js)

#### 온보딩 화면 (`OnboardingScreen.tsx`)
- **폴더 연결 전** 사용자에게 표시되는 3단계 가이드
- 시각적 계단식 인터페이스 (1, 2, 3 단계)
- 특징 소개 섹션 (로컬 폴더 지원, 스마트 태그, 다단계 계층, 빠른 검색)
- 시작하기 CTA 버튼
- Variant A 다크 스타일 적용: 슬레이트-9(#0e0e0e) + 그린 액센트(#4ade80)

#### 상단 필터 바 (`FilterBar.tsx`)
- **프로젝트 드롭다운**: 3개 프로젝트 중 선택
- **키워드 검색 입력**: 목표/카드 검색
- **통계 표시**: 현재 프로젝트 관련 목표 수
- 반응형 레이아웃 (모바일: 세로, 데스크톱: 가로)
- 다크 테마: `bg-slate-800`, `border-slate-600`

#### 좌측 목표 리스트 (`GoalList.tsx`)
- 선택된 프로젝트의 목표들을 세로로 나열
- 좌측 보더 강조 (선택 시: 그린, 비선택: 슬레이트)
- 클릭 시 중앙 카드 리스트 자동 갱신
- 각 목표의 카드 수를 표시

#### 중앙 카드 스크롤 영역 (`CardListScrollable.tsx`)
- **좌우 스크롤**: Horizontal scroll with 좌우 화살표 버튼
- **카드 제목만 표시**: 클릭 가능한 카드 버튼들
- **상태 표시**: 완료(green), 진행 중(yellow), 블로킹(red)
- **선택 상태**: 그린 액센트로 강조
- **자동 스크롤 감지**: 좌우 끝 도달 시 화살표 숨김

#### 우측 사이드 패널 (`CardDetailPanel.tsx`)
- 선택된 카드의 **상세 정보** 표시
- 카드 제목, 요약, 태그, 생성일, 상태 표시
- 태그는 클릭 가능 (향후 필터링 기능)
- "상세 내용 보기", "태그 편집" 액션 버튼
- 모바일에서는 바텀 시트로 표현

---

### 2️⃣ 상태 관리 시스템 (React)

#### 상태 변수 (`HomeContentV2.tsx`)
```typescript
- viewMode: 'onboarding' | 'main' (폴더 연결 전/후)
- selectedProjectId: 선택된 프로젝트 ID
- selectedGoalId: 선택된 목표 ID
- selectedCardId: 선택된 카드 ID
- searchQuery: 검색 키워드
```

#### 동작 흐름
1. **프로젝트 변경** → 목표 리셋, 카드 리셋, 검색 초기화
2. **목표 변경** → 카드 리셋 (해당 목표의 카드만 표시)
3. **검색 입력** → 목표/카드 필터링 (제목, 설명, 태그 포함)
4. **카드 선택** → 우측 패널에 상세 정보 표시

---

### 3️⃣ 임시 데이터 생성 (3개 프로젝트)

#### 프로젝트 구조
```
- Builder's Diary V2
  ├─ UI 레이아웃 재설계 (6 카드)
  ├─ 상태 관리 시스템 (5 카드)
  └─ 테스트 & 성능 최적화 (3 카드)

- Supabase 비용 최적화
  ├─ 비용 추적 시스템 (8 카드)
  ├─ 데이터베이스 성능 개선 (4 카드)
  ├─ 인증 & 보안 (3 카드)

- LLM 평가 및 최적화
  ├─ 모델 벤치마크 시스템 (6 카드)
  ├─ 파인튜닝 & 최적화 (5 카드)
  └─ 배포 & 모니터링 (7 카드)
```

**총 47개의 작업 카드** 생성

#### 카드 데이터 특성
- **제목**: 실제감 있는 작업 이름
- **요약**: 구체적인 작업 설명
- **태그**: 사용자의 기존 태그셋 활용
  - 비용감각
  - 검증규율
  - 계측설계
  - 문서보다실행흔적
  - 자기오류교정
- **상태**: `completed`, `in_progress`, `blocked`, (기본값)
- **생성일**: 실제 작업 시간대를 반영

---

### 4️⃣ 필터링 기능

#### 프로젝트 필터
- 드롭다운 선택으로 프로젝트 변경
- 선택 시 해당 프로젝트의 목표만 표시

#### 목표 필터
- 목표 리스트 클릭으로 선택
- 선택 시 해당 목표의 카드만 스크롤 영역에 표시

#### 키워드 검색
- 목표와 카드를 동시에 필터링
- 제목, 설명, 요약, 태그 내용 검색
- 실시간 필터링

---

### 5️⃣ Variant A 다크 스타일 유지

#### 컬러 팔레트
```css
--bg: #0e0e0e        /* 주 배경 */
--surface: #181818   /* 카드/패널 배경 */
--surface2: #222     /* 추가 깊이 */
--border: #2a2a2a    /* 보더 */
--text: #f0f0f0      /* 주 텍스트 */
--text2: #888        /* 보조 텍스트 */
--text3: #555        /* 약한 텍스트 */
--accent: #4ade80    /* 그린 액센트 */
```

#### 적용 요소
- 모든 배경색: 슬레이트-900, 슬레이트-800
- 텍스트: 슬레이트-100, 슬레이트-300, 슬레이트-500
- 상태 표시: 적색/황색/녹색 + 투명도 (opacity)
- 호버 효과: 슬레이트 아래로 1단계

---

## 📁 파일 구조

```
web/src/
├── app/
│   ├── v2/
│   │   └── page.tsx          # V2 페이지 진입점
│   └── globals.css           # (기존) 스타일 시스템
├── lib/
│   ├── mockData.ts           # ✨ 임시 데이터 (3개 프로젝트, 47개 카드)
│   └── types.ts              # (기존) 타입 정의
└── components/
    ├── HomeContentV2.tsx     # ✨ V2 메인 컴포넌트 (상태관리)
    ├── OnboardingScreen.tsx  # ✨ 온보딩 화면
    ├── FilterBar.tsx         # ✨ 상단 필터 바
    ├── GoalList.tsx          # ✨ 좌측 목표 리스트
    ├── CardListScrollable.tsx# ✨ 중앙 카드 스크롤
    ├── CardDetailPanel.tsx   # ✨ 우측 상세 패널
    └── (기타 기존 컴포넌트들)
```

✨ = 새로 생성된 파일

---

## 🎨 레이아웃 구조 (데스크톱)

```
┌─────────────────────────────────────────────────────────┐
│  FilterBar (프로젝트 선택, 키워드 검색, 통계)          │
├──────────┬───────────────────────┬─────────────────────┤
│ GoalList │ CardListScrollable    │  CardDetailPanel    │
│          │ (좌우 스크롤)          │  (카드 상세 정보)   │
│ 목표들   │ ← → ← → ← →           │                     │
│          │ [카드1] [카드2] ...    │  - 제목              │
│          │                       │  - 요약              │
│          │                       │  - 태그들            │
│          │                       │  - 생성일            │
└──────────┴───────────────────────┴─────────────────────┘

비율: 256px (좌) : 1fr (중) : 384px (우)
데스크톱 1200px 이상에서 전체 표시
태블릿/모바일에서 반응형 조정
```

---

## 🚀 접근 방법

### 페이지 접근
- **V1 (기존)**: `http://localhost:3000/`
- **V2 (신규)**: `http://localhost:3000/v2`

### 초기 흐름
1. V2 페이지 방문 → **온보딩 화면** 표시
2. "시작하기" 버튼 클릭 → **메인 레이아웃**으로 전환
3. 프로젝트 선택 → 목표 리스트 갱신
4. 목표 선택 → 카드 리스트 갱신
5. 카드 선택 → 우측 패널에 상세 정보 표시

---

## ✅ 완성된 기능

- [x] 온보딩 화면 (3단계 가이드)
- [x] 3단 레이아웃 (목표 + 카드 + 상세)
- [x] 프로젝트 드롭다운 필터
- [x] 키워드 검색 (실시간)
- [x] 좌우 스크롤 카드 리스트
- [x] 카드 상세 패널
- [x] 상태 표시 (완료/진행/블로킹)
- [x] 태그 표시 및 관리
- [x] 반응형 디자인 (모바일 하단 시트)
- [x] 다크 스타일 (Variant A)
- [x] 47개 실제감 있는 임시 데이터
- [x] 상태 관리 시스템

---

## 📝 다음 단계 (V2 → 프로덕션)

### Phase 1: Folder Connection
- [ ] File System API 연동 (로컬 폴더 선택)
- [ ] 폴더 구조 스캔 및 파싱
- [ ] 메타데이터 추출 (YAML front-matter)
- [ ] 실제 데이터 로드

### Phase 2: Persistence
- [ ] localStorage에 선택 상태 저장
- [ ] URL 쿼리 스트링 동기화 (공유 가능)
- [ ] 세션 복구 기능

### Phase 3: Enhancement
- [ ] 태그 기반 고급 필터링
- [ ] 카드 내용 마크다운 렌더링
- [ ] 카드 편집/생성 기능
- [ ] 내보내기 (PDF, JSON)

### Phase 4: Performance
- [ ] 가상 스크롤 (카드 많을 때)
- [ ] 이미지 최적화
- [ ] 캐싱 전략
- [ ] 번들 크기 최적화

---

## 🔍 검증 방법

### 개발 서버 실행
```bash
cd /Users/taegyujeong/work/builders-diary/web
npm run dev
```

### V2 페이지 방문
```
http://localhost:3000/v2
```

### 테스트 흐름
1. "시작하기" 클릭 → 메인 레이아웃 확인
2. 프로젝트 드롭다운 → "Builder's Diary V2" 선택
3. 목표 리스트 → "UI 레이아웃 재설계" 클릭
4. 카드 스크롤 → 좌우 화살표로 스크롤
5. 카드 선택 → 우측 패널에 상세 정보 표시
6. 검색 → "파인튜닝" 입력 → LLM 평가 프로젝트의 카드만 표시

---

## 💡 설계 철학

### 사용자 중심
- **온보딩**: 첫 사용자도 직관적으로 이해 가능
- **계층 구조**: 프로젝트 → 목표 → 카드의 명확한 흐름
- **빠른 탐색**: 우측 패널로 클릭 시 즉시 상세 정보

### 시각적 명확성
- **Variant A 다크 스타일**: 장시간 사용에도 편안함
- **상태 표시**: 완료/진행/블로킹을 색상으로 구분
- **선택 강조**: 그린 액센트로 현재 선택 상태 명시

### 성능 고려
- **임시 데이터**: 실제 폴더 로드 전 UI 테스트 가능
- **효율적인 필터링**: React의 useCallback으로 메모이제이션
- **반응형**: 모바일/태블릿/데스크톱 모두 최적화

---

## 📊 데이터 통계

- **프로젝트**: 3개
- **목표**: 10개
- **작업 카드**: 47개
- **태그 유형**: 5개
- **상태 분포**:
  - 완료: 11개
  - 진행 중: 15개
  - 블로킹: 2개
  - 대기: 19개

---

## 🎓 학습 포인트

### React State Management
- 복잡한 상태 흐름을 prop drilling 없이 관리
- useCallback으로 불필요한 리렌더링 방지

### UI/UX Design
- 다단 레이아웃의 명확한 영역 분할
- 스크롤 가능한 콘텐츠의 시각적 안내 (화살표)

### Tailwind CSS
- 다크 테마 구축 시 일관된 컬러 팔레트 유지
- 반응형 디자인 (`md:`, `lg:` 브레이크포인트)

### Component Reusability
- 각 컴포넌트가 단일 책임 원칙 준수
- Props를 통한 유연한 커스터마이제이션

---

## 📞 문의 및 개선

이 V2 프로토타입은 **폴더 연결 후 실제로 보고 싶은 UI 경험**을 구현했습니다. 
실제 폴더 데이터와 통합될 때 추가 기능이나 개선사항이 필요하면 언제든 업데이트할 수 있습니다.

---

**작성일**: 2026-09-04  
**상태**: 완성 ✅  
**배포 준비**: V2 페이지 (`/v2`)에서 즉시 테스트 가능
